[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "md-extract"
dynamic = ["version"]
description = "Extract structured sections from Markdown by header — bracket-style access with robust handling of code blocks, tables, math, and YAML front matter."
readme = "README.md"
requires-python = ">=3.8"
license = { file = "LICENSE" }
authors = [
    { name = "Fasil", email = "fasilwdr@hotmail.com" },
]
keywords = ["markdown", "parser", "extractor", "headers", "sections", "ast"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Text Processing :: Markup :: Markdown",
    "Typing :: Typed",
]
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7", "lxml>=4.9", "build", "twine"]
xpath = ["lxml>=4.9"]

[project.urls]
Homepage = "https://github.com/fasilwdr/MD-Extract"
Documentation = "https://github.com/fasilwdr/MD-Extract#readme"
Source = "https://github.com/fasilwdr/MD-Extract"
Issues = "https://github.com/fasilwdr/MD-Extract/issues"
Changelog = "https://github.com/fasilwdr/MD-Extract/releases"

[tool.setuptools.dynamic]
version = { attr = "md_extract.__version__" }

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
md_extract = ["py.typed"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
